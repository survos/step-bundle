{# file: templates/step/action/display_code.html.twig #}
<div class="action action-code card shadow-sm mb-3">
  <div class="card-body">
    {% if note %}<p class="mb-2">{{ note }}</p>{% endif %}
    <pre class="code-block"><code class="language-{{ lang|default('text') }}">{{ code }}</code></pre>
  </div>
</div>
